/**
 * 
 * @param {*} num 
 * 
 * 3 types of steps -1, divide by 3, divide by 2
 */
function minStepsToOne(num) {
    const distances = { [num]: 0}

    return x is 
}


// print the result of minStepsToOne: the path

// what if you don't have -steps - 1
// check if it exists

// dp:

// tabulation, just look at the previous problem
function minStepsToOneTabulation(num) {
    const distances = Array(n + 1);
    
    // foundation
    distances[1] = 0;
    for (let i = 2; i < distances.length; i += 1) {

    }
}


// magic approach
